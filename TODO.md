# TODO: Divi Post Carousel

## Post Carousel Module Issues
- Design in mobile view doesn't match the shortcode implementation
- CSS classes are implemented correctly but some styling differences remain
- Need to investigate differences in HTML structure or class arrangement
- Button styling on mobile needs to match the shortcode version

## Future Improvements
- Create a dedicated custom carousel module with manual slides
- Unify mobile and desktop styling between module and shortcode
- Ensure responsive behavior is consistent across implementations
- Review initialization script for potential differences

## Notes
- Current workaround: use shortcode implementation when mobile styling is critical
- The autoplay setting has been fixed to be off by default 